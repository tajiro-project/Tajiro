<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.tajiro.auth.mapper.AuthMapper">

    <!-- 1. 이메일로 사용자 조회 -->
    <select id="findByEmail" parameterType="string" resultType="UserVO">
        SELECT
            id,
            email,
            password_hash AS passwordHash,
            name,
            role,
            phone,
            agency_name   AS agencyName,
            provider,
            status,
            created_at    AS createdAt,
            deleted_at    AS deletedAt
        FROM users
        WHERE email = #{email}
    </select>

    <!-- 2. 회원 생성 -->
    <insert id="insertUser" parameterType="UserVO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password_hash, name, role, phone, agency_name, provider, status, created_at)
        VALUES (#{email}, #{passwordHash}, #{name}, #{role}, #{phone}, #{agencyName}, #{provider}, #{status}, NOW())
    </insert>

    <!-- 3. 필수 약관 ID 목록 조회 -->
    <select id="selectRequiredTermsIds" resultType="long">
        SELECT id
        FROM terms
        WHERE required = 1
          AND effective_at &lt;= NOW()
    </select>

    <!-- 4. 약관 동의 이력 저장 (agreed_at, terms_version은 저장 시점 기준) -->
    <insert id="insertTermsConsent">
        INSERT INTO terms_consent (user_id, terms_id, agreed, terms_version, agreed_at)
        SELECT #{userId}, id, #{agreed}, version, NOW()
        FROM terms
        WHERE id = #{termsId}
    </insert>

    <!-- 5. 회원 탈퇴 (soft-delete) -->
    <update id="softDeleteUser">
        UPDATE users
        SET status = 'WITHDRAWN',
            deleted_at = NOW()
        WHERE id = #{userId}
    </update>

    <!-- 6. 탈퇴한 계정의 이메일 반납 (재가입 시, 기존 탈퇴 row의 이메일을 변형해 원래 이메일을 재사용 가능하게 함) -->
    <update id="releaseWithdrawnEmail">
        UPDATE users
        SET email = CONCAT('withdrawn_', UNIX_TIMESTAMP(), '_', email)
        WHERE id = #{userId}
    </update>

</mapper>
